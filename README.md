# Pushed by AI/Works

This file was generated by the **AI/Works push-to-git** component as a
demo of the diff + push flow.

## What this proves

- The push-to-git backend can read artifacts (currently hardcoded demo
  content; eventually fetched from the kernel)
- The diff preview correctly distinguishes added vs modified files
- The git push lands the file on the target branch with proper
  attribution to the GitHub user who clicked Sign in
